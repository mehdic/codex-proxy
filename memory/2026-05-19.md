# 2026-05-19

## Durable memories

- The new Mac mini `openclaw-app-mini` is now the paired and connected OpenClaw node/execution host, running OpenClaw `2026.5.18`.
- The main Mac mini Gateway was updated to OpenClaw `2026.5.18`; the old disconnected `Mehdi Mac Node` entry was removed from the legacy registry.
- `openclaw-app-mini` runs the OpenClaw node LaunchAgent (`ai.openclaw.node`) and should remain the execution host, not a second Gateway.
- Passwordless sudo on the main Mac mini is intentionally enabled via `/etc/sudoers.d/openclaw-bootstrap` so provisioning and maintenance do not require repeated password prompts.
- OrbStack start-at-login was enabled on `openclaw-app-mini`; all migrated containers/VMs are expected to run there.
- Media storage should stay on the old Mac’s external `/Volumes/Media` disk, exposed lightly over SSH/SFTP only; the new Mac consumes it via Docker-native SSHFS volumes (`vieux/sshfs`).
- The working media volume mappings are `openclaw-media-downloads -> /Volumes/Media/Downloads` and `openclaw-media-tv -> /Volumes/Media/TV Shows`.
- Sonarr and Jackett were recreated against the SSHFS-backed media volumes and verified able to read/write the mounted media paths.
- Public service routing was cut over to the new Mac Tailscale IP `100.77.202.98` for the app services that were moved there, including `ha`, `n8n`, `sms`, and `sonarr`.
